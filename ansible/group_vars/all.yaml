---
project_name: "projet-ops"

# PostgreSQL
db_host:     "172.40.0.100"
db_port:     "5432"
db_name:     "appdb"
db_user:     "appuser"
db_password: "{{ lookup('env', 'DB_PASSWORD') }}"

# HAProxy — liste des serveurs web pour le template haproxy.cfg.j2
haproxy_web_servers:
  - name: web1
    ip:   "172.30.0.10"
    port: 80
  - name: web2
    ip:   "172.30.0.11"
    port: 80

# Apache
apache_doc_root: "/var/www/html"